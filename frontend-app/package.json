{
  "name": "frontend-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npx nx serve frontend-app",
    "build": "npx nx build frontend-app",
    "test": "npx nx test frontend-app",
    "lint": "npx nx lint frontend-app"
  }
}
