{
  "name": "frontend-admin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npx nx serve frontend-admin",
    "build": "npx nx build frontend-admin",
    "test": "npx nx test frontend-admin",
    "lint": "npx nx lint frontend-admin"
  }
}
